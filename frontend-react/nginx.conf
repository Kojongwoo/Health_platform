# frontend-react/nginx.conf

server {
    listen 80;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # 요청한 파일이 없을 경우, index.html로 돌려보내 React Router가 처리하도록 함
        try_files $uri $uri/ /index.html;
    }
}